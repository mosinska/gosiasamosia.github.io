switch (wyrazenie) {
    case przypadek 1: //fragment wykonany gdy rezultat wyrazenia jest równy rezultat1 - potrzebuje break
    break;

    case przypadek: //fragment wykonany gdy rezultat wyrazenia jest równy rezultat2 - potrzebuje break
    break;

}
